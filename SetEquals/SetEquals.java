import java.util.Set;

public class SetEquals {

    // Fonction pour vérifier si deux ensembles sont égaux
    public static boolean areSetsEqual(Set<String> set1, Set<String> set2) {
        return set1.equals(set2);  // Utilise la méthode equals pour comparer les deux ensembles
    }
}
